{
  "name": "@rauf/cli",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "bin": {
    "rauf": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rauf/core": "workspace:*",
    "@rauf/loop": "workspace:*",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.2.0",
    "vitest": "^3.0.0",
    "typescript": "^5.7.0",
    "eslint": "^9.0.0"
  }
}
