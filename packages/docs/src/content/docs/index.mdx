---
title: What is Rauf?
description: A CLI + web tool for installing, managing, and monitoring autonomous coding loops.
template: splash
hero:
  tagline: Install, manage, and monitor autonomous coding loops across local software projects. Rauf provides backlog management, status dashboards, and installation wizards — all powered by Claude Code.
  actions:
    - text: Get Started
      link: getting-started/your-first-loop/
      icon: right-arrow
    - text: Core Concepts
      link: getting-started/core-concepts/
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## Key Features

<CardGrid stagger>
  <Card title="Project Installation" icon="setting">
    Auto-detect tech stack (Node.js, Python, Go, Rust) and install loop infrastructure with a single
    command.
  </Card>
  <Card title="Backlog Management" icon="list-format">
    Add, edit, delete, and list work items with priorities, acceptance criteria, and dependencies —
    via CLI or web UI.
  </Card>
  <Card title="Status Monitoring" icon="approve-check-circle">
    Every observer — `status`, `follow`, `log`, and the web — reconstructs loop activity from files
    on disk (`state.json` + `events.ndjson`). Foreground and detached runs look identical.
  </Card>
  <Card title="Web Dashboard" icon="laptop">
    React SPA with project cards, backlog CRUD, a live status + event view, and recovery actions —
    all reading the same on-disk substrate as the CLI.
  </Card>
</CardGrid>

## How It Works

1. You define work items in a **backlog** (JSON-based task queue)
2. The loop runner picks the next item, spawns a Claude Code session, and monitors the result
3. Each iteration reads acceptance criteria, implements changes, runs verification, and commits
4. The loop continues until the backlog is empty or a limit is reached

![One rauf iteration: select an item, build the prompt, spawn the agent, parse its signal, verify and commit, then advance to the next item.](./images/loop-lifecycle.svg)

## Quick Install

```bash
# Clone and build
git clone https://github.com/garygentry/rauf.git
cd rauf && pnpm install && pnpm build

# Install into a project
rauf install ~/workspace/my-project --yes

# Start the loop
rauf loop run ~/workspace/my-project
```
