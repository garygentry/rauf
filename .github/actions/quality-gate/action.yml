name: Quality Gate
description: The canonical gate — `pnpm gate` (build + schema:check + version:check + typecheck + lint + format:check + test). Single source of truth shared by ci.yml, release.yml, the forge pipeline, and local dev so they can never drift.
runs:
  using: composite
  steps:
    - run: pnpm gate
      shell: bash
