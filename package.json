{
  "name": "rauf",
  "version": "0.8.1",
  "description": "Management tool for rauf autonomous coding loops",
  "private": true,
  "//publish": "The published npm package is the launcher `@garygentry/rauf` in npm-dist/ (a thin binary bootstrapper; bare `rauf` is blocked by npm's similarity filter). This workspace root is NOT published; its version is kept in lockstep (scripts/check-versions.ts + scripts/release/lib.ts) only as the canonical monorepo version.",
  "repository": {
    "type": "git",
    "url": "https://github.com/garygentry/rauf.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test && vitest run",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "dev": "pnpm --filter @rauf/web dev",
    "dev:docs": "pnpm --filter @rauf/docs dev",
    "build:docs": "pnpm --filter @rauf/docs build",
    "compile": "pnpm build && bun build --compile scripts/binary-entry.ts --outfile rauf-bin",
    "dogfood:runner": "pnpm compile && bash scripts/install-binary.sh --local --name rauf-stable",
    "clean": "pnpm -r clean",
    "generate:schemas": "bun run scripts/generate-json-schemas.ts",
    "schema:check": "bun run scripts/generate-json-schemas.ts --check",
    "version:check": "bun run scripts/check-versions.ts",
    "check:docs": "bun run scripts/check-docs.ts",
    "gate": "pnpm build && pnpm schema:check && pnpm version:check && pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm check:docs",
    "generate:diagrams": "bun run scripts/generate-diagrams.ts",
    "release:prepare": "bun run scripts/release/prepare.ts",
    "rauf": "rauf"
  },
  "devDependencies": {
    "@rauf/cli": "workspace:*",
    "@eslint/js": "^9.39.3",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.56.0",
    "vitest": "^3.0.0",
    "zod-to-json-schema": "^3.25.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
